---
title: TypeScript notes from the field
date: "2026-04-05"
description: A short fake article about narrowing, generics, and the kinds of mistakes that only show up at compile time.
tags:
  - TypeScript
---

TypeScript is not a separate language from JavaScript; it is a lens. The compiler catches whole classes of bugs before a user ever hits refresh. That does not mean every line needs a heavy type—sometimes `unknown` and a guard are the honest choice.

This entry is filler text for the portfolio blog scaffold. Replace it with a real story about a bug you fixed, a pattern you adopted, or an API you wrestled with.

## Ideas for a real version

1. A concrete bug and the type-level fix  
2. When you reached for `satisfies` instead of a wide annotation  
3. How you structure types in a small app versus a shared library  
